// bir stringin hepsinin küçük harf olup olmadığını kontrol eden program

bool isLowerCase(String str) {
  return str == str.toLowerCase();
}

bool isUpperCase(String str) {
  return str == str.toUpperCase();
}
